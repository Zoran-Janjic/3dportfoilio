import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        I am a Software Engineer with expertise in MERN, Python, Kotlin, Kobweb,
        and various AI technologies including Nvidia Nemo ASR and OpenAI
        Whisper. At my current position as a Software Engineer, I focus on
        enhancing language-speaking capabilities through AI-driven analysis,
        emphasizing precision in pronunciation and fluency. I excel in codebase
        refactoring for efficiency and transparency, and I am proficient in
        writing rigorous unit tests using PyTest.
      </motion.p>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        Additionally, I integrate REST APIs using FAST API with our AI audio
        analysis system. I am deeply interested in AI, machine learning, and
        computer networks. I enjoy developing seamless user interfaces on the
        front end and designing scalable APIs on the back end. In my free time,
        I work on personal coding projects and experiment with AI models and
        computer networks. I value collaboration and continuously seek
        opportunities for growth and learning. I am driven by a passion for
        technology and a commitment to creating innovative solutions in various
        AI domains and full-stack development.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
